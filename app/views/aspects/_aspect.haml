%li
  %h4
    = link_to aspect, aspect
    %span
      -# by star, change the count of contacts
      -#= t('contacts', :count => contact_count)
      = t('contacts', :count => aspect.visible_users.count - 1)

  -#- if contact_count > 0
    -#- for hash in contacts
      -#= person_image_link(hash[:person])
  - users = aspect.visible_users
  - users.delete(current_user)
  - for user in users
    = person_image_link(user.person)
